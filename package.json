{
  "name": "caitlin-fields-portfolio",
  "private": true,
  "version": "1.0.0",
  "description": "Tooling for the portfolio. The website itself has no build step; this is only used to export the PDF.",
  "type": "module",
  "scripts": {
    "pdf": "node tools/export-pdf.mjs"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
