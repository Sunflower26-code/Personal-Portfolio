# Tooling for the PDF export only. The website itself has no build step.
node_modules/
package-lock.json

# LaTeX build artifacts from compiling the resume locally.
*.aux
*.log
*.fls
*.fdb_latexmk
*.synctex.gz
*.out
